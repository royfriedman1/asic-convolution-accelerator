# ASIC Verification & Analysis Suite — dependencies
# Tested with Python 3.11 on Windows 11 (conda env: varification_model)
PyQt6>=6.5.0,<7.0       # tested: 6.6.1
numpy>=1.24.0,<3.0      # tested: 2.3.5  (NumPy 2.x supported)
matplotlib>=3.7.0       # tested: 3.10.7

# Optional but recommended
Pillow>=9.0.0           # image loading (fallback if OpenCV absent) — tested: 12.0.0
opencv-python>=4.8.0    # video loading + fast image I/O             — tested: 4.13.0
